# [PLAN-NNNN] Title

**Implements:** ADR-NNNN (link once that ADR is `Accepted` — a plan
shouldn't be written against a still-`Proposed` decision, see
`docs/plans/README.md`)

**Status:** Not Started | In Progress | Blocked | Done

**Last updated:** YYYY-MM-DD

## Goal

One or two sentences: what does "done" look like, in concrete terms a
reader could verify without reading the whole plan.

## Scope

What's explicitly in this plan, and — as important — what's explicitly
deferred or out of scope. Cross-reference the ADR's Decision Outcome
rather than restating it.

## Tasks

Group tasks logically (new files, changes to existing files, tests, docs)
rather than one flat list. Check items off as they're completed — this
section is the living, mutable part of the plan.

- [ ] Task one
- [ ] Task two
  - [ ] Sub-task, if a task is large enough to need one

## Critical files

New:
- `path/to/new/File.cs`

Modified:
- `path/to/existing/File.cs`

## Test plan

What gets tested, and how — unit vs. integration, what's newly covered
vs. what's regression coverage for existing behavior.

## Verification

Concrete steps to confirm this actually works end-to-end once the tasks
above are done — not just "tests pass," but anything this repo's
established pattern calls for (e.g. a real manual smoke test for
networking/session-facing changes).

## Open questions / blockers

Anything still unresolved that isn't blocking starting the work, but
should be tracked. Delete this section if there aren't any.
